/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      loader: 'akamai',
      path: '',
    },
    experimental: { appDir: true }

  };

  
  
  module.exports = nextConfig;
  