import React from 'react';
import Layout from './layout';

const Teams: React.FC = () => {
  return (
    <Layout>
      <h1>Fixture Difficulty Tracker</h1>
      {/* Additional content for the Teams page */}
    </Layout>
  );
};

export default Teams;
